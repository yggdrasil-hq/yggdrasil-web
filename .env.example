# Set by root deploy compose in full-stack dev
NEXT_PUBLIC_BASE_PATH=/app

# REST API base (via nginx at /api)
NEXT_PUBLIC_API_BASE_URL=/api

# Server-side API URL (Next.js middleware / RSC — docker network)
API_INTERNAL_URL=http://api:3000
