# Set by root deploy compose in full-stack dev
NEXT_PUBLIC_BASE_PATH=/app

# REST API base. Under /app in docker dev so MSW (scoped to /app/) can intercept.
# Switch to /api when wiring the real API through nginx.
NEXT_PUBLIC_API_BASE_URL=/app/api
